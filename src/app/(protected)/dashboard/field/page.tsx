import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { requireActiveBrand } from "@/lib/auth";
export default async function FieldPage(){const{supabase,userId}=await requireActiveBrand();const{data:missions}=await supabase.from("missions").select("id,title,status,mission_type,scheduled_start_at,report_due_at,address_snapshot").eq("assigned_user_id",userId).is("archived_at",null).order("scheduled_start_at").limit(30);return <div className="mx-auto max-w-xl space-y-5"><div><h1 className="text-2xl font-semibold">Mon terrain</h1><p className="text-muted-foreground">Missions à accepter, du jour et rapports attendus.</p></div>{(missions??[]).map((mission)=><Link href={`/dashboard/missions/${mission.id}`} key={mission.id}><Card className="mb-3 transition hover:border-primary"><CardHeader className="pb-2"><div className="flex justify-between gap-2"><CardTitle className="text-base">{mission.title}</CardTitle><Badge variant={mission.status==="report_pending"?"destructive":"secondary"}>{mission.status}</Badge></div></CardHeader><CardContent className="text-sm"><p>{mission.scheduled_start_at?new Date(mission.scheduled_start_at).toLocaleString("fr-FR"):"À planifier"}</p><p className="text-muted-foreground">{String((mission.address_snapshot as Record<string,string>|null)?.city??"")} · {mission.mission_type}</p></CardContent></Card></Link>)}{!missions?.length&&<p className="rounded-md border p-6 text-center text-muted-foreground">Aucune mission affectée.</p>}</div>}
