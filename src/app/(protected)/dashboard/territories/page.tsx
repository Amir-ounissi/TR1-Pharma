import { TerritoryForm } from "@/components/reference/simple-forms";
import Link from "next/link";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { requireActiveBrand } from "@/lib/auth";

export default async function TerritoriesPage() { const { supabase, brand } = await requireActiveBrand(); const [{ data: territories }, { data: brandRecord }] = await Promise.all([supabase.from("territories").select("*").eq("brand_id", brand.id).is("archived_at", null).order("name"), supabase.from("brands").select("organization_id").eq("id", brand.id).single()]); return <div className="space-y-6"><div><h1 className="text-2xl font-semibold tracking-tight">Territoires</h1><p className="text-muted-foreground">Zones commerciales propres à {brand.name}.</p></div><div className="grid gap-6 xl:grid-cols-[1fr_420px]"><Card><CardHeader><CardTitle>Territoires actifs</CardTitle></CardHeader><CardContent className="p-0"><Table><TableHeader><TableRow><TableHead>Nom</TableHead><TableHead>Type</TableHead><TableHead>Région</TableHead><TableHead>Département</TableHead><TableHead>Codes postaux</TableHead></TableRow></TableHeader><TableBody>{(territories ?? []).map((territory) => <TableRow key={territory.id}><TableCell className="font-medium"><Link href={`/dashboard/territories/${territory.id}`} className="hover:underline">{territory.name}</Link></TableCell><TableCell>{territory.territory_type}</TableCell><TableCell>{territory.region_code || "—"}</TableCell><TableCell>{territory.department_code || "—"}</TableCell><TableCell>{territory.postal_codes?.join(", ") || "—"}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card><Card><CardHeader><CardTitle>Nouveau territoire</CardTitle></CardHeader><CardContent>{brandRecord ? <TerritoryForm organizationId={brandRecord.organization_id} /> : <p className="text-destructive">Organisation introuvable.</p>}</CardContent></Card></div></div>; }
